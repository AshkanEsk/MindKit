import React from 'react';
import Select from './select';

const AddNew: React.FC = () => {
    return (
        <div>
            <Select titles={['title1', 'title2']} />
        </div>
    );
};

export default AddNew;
